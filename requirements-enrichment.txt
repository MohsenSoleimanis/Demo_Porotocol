# FMLS enrichment + extraction pipeline dependencies.
# Verified compatible on Python 3.12, Linux + CUDA 12.x (L4).
# Generated 2026-05-22.

# === Meta-model (USDM v4) ===
usdm>=0.67.0

# === LLM extraction ===
openai>=1.50.0
instructor>=1.6.0
python-dotenv>=1.0.0

# === Schema / data ===
pydantic>=2.5.0
numpy>=1.26.0
pandas>=2.0.0

# === NER + entity priming ===
gliner>=0.2.10

# === Clinical NLP (negation, assertion, section detection) ===
medspacy>=1.3.1
# scispaCy linker (alternative to MedCAT when no UMLS license available)
scispacy>=0.5.4

# === Coreference resolution ===
fastcoref>=2.1.6

# === Embeddings (chunk vectors + alias clustering) ===
sentence-transformers>=3.0.0
# transformers + torch pulled in by gliner/fastcoref/sentence-transformers
# but pin explicitly so we know what we ship
transformers>=4.45.0
torch>=2.4.0
huggingface-hub>=0.25.0

# === Language detection + sentence segmentation ===
langdetect>=1.0.9
pysbd>=0.3.4

# === Temporal extraction (pure Python alternative to HeidelTime) ===
dateparser>=1.2.0

# === Number / unit utilities ===
text2num>=2.5.1

# === PII detection ===
presidio-analyzer>=2.2.355
presidio-anonymizer>=2.2.355

# === Boilerplate / near-duplicate detection ===
simhash>=2.1.2

# === CDISC conformance validation ===
cdisc-rules-engine>=0.13.0

# === Spacy language models (downloaded via spacy CLI, not pip) ===
# python -m spacy download en_core_web_sm
# python -m scispacy download en_core_sci_md
